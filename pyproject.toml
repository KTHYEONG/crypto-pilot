[tool.pyright]
exclude = ["data", "legacy", "logs", "results", ".pytest_cache", ".git"]

[tool.ruff]
exclude = ["data", "legacy", "logs", "results", ".pytest_cache", ".git"]

[tool.isort]
skip = ["data", "legacy", "logs", "results"]
